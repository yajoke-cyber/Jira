import  { ReactNode } from 'react';
// import { AuthProvider } from 'context/auth-context';
// export const AppProviders =({children}:{children:ReactNode})=>{
//     return ( <AuthProvider>
//     {children}
//     </AuthProvider> )
// } 
//在这里注意children为AuthProvider中的props.child属性，因此需要做类型限定